{
  "name": "coinpulse",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix server ci && npm --prefix client ci && npm --prefix client run build && npm --prefix server run build && node scripts/copy-client-build.mjs",
    "test": "npm --prefix server run test",
    "start": "npm --prefix server run start"
  }
}
